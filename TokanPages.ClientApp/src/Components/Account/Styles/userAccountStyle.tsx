import { makeStyles } from "@material-ui/core/styles";
import { CustomColours } from "../../../Theme/customColours";

const userAccountStyle = makeStyles(() => (
{
    section: 
    {
        backgroundColor: CustomColours.colours.white
    }
}));

export default userAccountStyle;
