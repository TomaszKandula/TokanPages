import { makeStyles } from "@material-ui/core/styles";

const menuStyle = makeStyles(() => (
{
    menuBackground:
    {
        width: 300
    },   
    drawerContainer: 
    {
        width: 300
    }
}));

export default menuStyle;
