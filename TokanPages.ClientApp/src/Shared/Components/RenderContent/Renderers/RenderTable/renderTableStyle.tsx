import { makeStyles } from "@material-ui/core";

export const RenderTableStyle = makeStyles(() => ({
    table: {
        minWidth: 650,
    },
    header: {
        textAlign: "left",
    },
    row: {
        textAlign: "left",
    },
}));
