import { makeStyles } from "@material-ui/core/styles";

export const RenderNavbarMenuStyle = makeStyles(() => ({
    list: {
        display: "flex",
        flexDirection: "row",
        flexWrap: "nowrap",
        justifyContent: "space-between",
    },
}));
