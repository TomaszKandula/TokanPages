export enum OperationStatus 
{
    notStarted,
    inProgress,
    hasFinished,
    hasFailed
}

export enum IconType
{
    info,
    warning,
    error,
}

export enum DialogType
{
    toast,
    box
}

export enum UserMedia
{
    notSpecified,
    userImage,
    userVideo
}
